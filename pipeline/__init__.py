# pipeline/__init__.py

# You can import the main agent runner function to make it easier to call
from .agent_runner import run_agent_with_name

# Optionally, you can import other key modules if you want them to be available at the package level
